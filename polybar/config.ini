;==========================================================
;
;
;   ██████╗  ██████╗ ██╗  ██╗   ██╗██████╗  █████╗ ██████╗
;   ██╔══██╗██╔═══██╗██║  ╚██╗ ██╔╝██╔══██╗██╔══██╗██╔══██╗
;   ██████╔╝██║   ██║██║   ╚████╔╝ ██████╔╝███████║██████╔╝
;   ██╔═══╝ ██║   ██║██║    ╚██╔╝  ██╔══██╗██╔══██║██╔══██╗
;   ██║     ╚██████╔╝███████╗██║   ██████╔╝██║  ██║██║  ██║
;   ╚═╝      ╚═════╝ ╚══════╝╚═╝   ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝
;
;
;   To learn more about how to configure Polybar
;   go to https://github.com/polybar/polybar
;
;   The README contains a lot of information
;
;==========================================================

[colors]
background = #111111CC
background-alt = #373B41
foreground = #ffffff
primary = #7359F8
secondary = #8ABEB7
alert = #A54242
disabled = #707880

[bar/statusbar]
width = 100%
height = 40pt
radius = 6

; dpi = 96

background = ${colors.background}
; foreground = ${colors.foreground}

line-size = 3pt

border-size = 4pt
border-color = #00000000

padding-left = 0
padding-right = 1

module-margin = 1

; separator = 
separator-foreground = ${colors.disabled}

font-0 = Hack Nerd Font:size=18

modules-left = xworkspaces xwindow
modules-center = date
modules-right = filesystem pulseaudio xkeyboard memory cpu battery wlan eth 

cursor-click = pointer
cursor-scroll = ns-resize

enable-ipc = true

; tray-position = right

; wm-restack = generic
; wm-restack = bspwm
wm-restack = i3

; override-redirect = true

[module/xworkspaces]
type = internal/xworkspaces

label-active = %name%
label-active-background = ${colors.background-alt}
label-active-underline= ${colors.primary}
label-active-padding = 1

label-occupied = %name%
label-occupied-padding = 1

label-urgent = %name%
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

label-empty = %name%
label-empty-foreground = ${colors.disabled}
label-empty-padding = 1

[module/xwindow]
type = internal/xwindow
label = %title:0:60:...%

[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /
format-prefix = ""
label-mounted =   %free%
label-mounted-foreground = ${colors.primary}
label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}

[module/pulseaudio]
type = internal/pulseaudio

format-volume-prefix = "󰕾 "
format-volume-prefix-foreground = ${colors.primary}
format-volume = <label-volume>

label-volume =  %percentage%%

label-muted = muted
label-muted-foreground = ${colors.disabled}

[module/xkeyboard]
type = internal/xkeyboard
blacklist-0 = num lock

label-layout = %layout%
label-layout-foreground = ${colors.primary}

label-indicator-padding = 2
label-indicator-margin = 1
label-indicator-foreground = ${colors.background}
label-indicator-background = ${colors.secondary}

[module/memory]
type = internal/memory
interval = 2
format-prefix = "󰫩 "
format-prefix-foreground = ${colors.primary}
label =  %percentage_used:2%%

[module/cpu]
type = internal/cpu
interval = 2
format-prefix = " "
format-prefix-foreground = ${colors.primary}
label =  %percentage:2%%


[module/battery]
type = internal/battery
battery = BAT0

adapter = ADP1

full-at = 98

format-charging = 󰂅 <label-charging>

label-charging = %percentage:2%%

format-charging-padding = 1

format-charging-foreground = ${colors.foreground}

format-discharging = <ramp-capacity> <label-discharging>

label-discharging = %percentage%%

format-discharging-foreground = ${colors.foreground}

format-full-prefix = " "

ramp-capacity-0 = " "

ramp-capacity-1 = " "

ramp-capacity-2 = " "

ramp-capacity-3 = " "

ramp-capacity-4 = " "

ramp-capacity-0-foreground = ${colors.red}

animation-charging-framerate = 910

format-discharging-padding = 1

poll-interval = 3

click-left = exec ~/.config/rofi/launchers/type-1/launcher.sh




[network-base]
type = internal/network
interval = 5
format-connected = <label-connected>
format-disconnected = <label-disconnected>
label-disconnected = %{F#F0C674}%ifname%%{F#707880} disconnected

[module/wlan]
inherit = network-base
interface-type = wireless
label-connected = 󰤨 %{F#7359F8} %essid%

[module/eth]
inherit = network-base
interface-type = wired
label-connected = %{F#F0C674}%ifname%%{F-} %local_ip%

[module/date]
type = internal/date
interval = 1

date = %H:%M
date-alt = %Y-%m-%d %H:%M:%S

label = %date%
label-foreground = ${colors.primary}

[settings]
screenchange-reload = true
pseudo-transparency = true

; vim:ft=dosini




; ;; Application Launcher Module
; [module/launcher]
; type = custom/text

; content = 異
; content-background = black
; content-foreground = green

; click-left = ~/.config/rofi/launchers/type-1/launcher.sh
; click-right = launcher_t1

; ;; Power Menu Module
; [module/powermenu]
; type = custom/text

; battery = BAT0  # Replace with your battery name if different
; adapter = AC    # Replace with your adapter name if different
; format = "%percentage%%"  # Displays only percentage
; format-prefix = "󰫩 "
; format-prefix-foreground = ${colors.primary}



; click-left = ~/.config/rofi/powermenu/type-1/powermenu.sh
; click-right = powermenu_t1

; Adjust these values if necessary based on your system output of `ls -l /sys/class/power_supply/`

; Set a threshold for low battery notification (optional)
; low-at = 10
; Customize the format string for battery display
; Alternative formats with charging/discharging status and optional full indicator
; format-charging = <label-charging> %percentage%%
; format-discharging =  %percentage%%
; format-full = <label-full> Batt. Full - %percentage%%

; Customize labels for different battery states (optional)
; label-charging = Batt. Charging: 
; label-discharging = Batt. Discharging: 
; label-full = Batt. Full: 

; Set the polling interval to refresh battery information (optional)
; Default: 5 seconds
; poll-interval = 10
